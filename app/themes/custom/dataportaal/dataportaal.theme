<?php
    /**
     * Implements hook_preprocess_block().
     */
    function dataportaal_preprocess_node(&$variables) {

    	$host = \Drupal::request()->getSchemeAndHttpHost();

    	if ($hostPrefix === "acc"){
    		$host = "https://acc.data.amsterdam.nl";
    	}

    	if ($hostPrefix === "dat"){
    		$host = "https://acc.data.amsterdam.nl";
    	}

      $variables['host'] = $host;
    }

